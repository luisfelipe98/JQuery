//valida se é emailfunction isEmail(email){    var exclude=/[^@\-\.\w]|^[_@\.\-]|[\._\-]{2}|[@\.]{2}|(@)[^@]*\1/;    var check=/@[\w\-]+\./;    var checkend=/\.[a-zA-Z]{2,3}$/;    if(((email.search(exclude) != -1)||(email.search(check)) == -1)||(email.search(checkend) == -1)){return false;}    else {return true;}}//retira os espaços em brancos de uma string, tanto à direita quanto à esquerdafunction trim (str) {	return str.replace(/^\s+|\s+$/g, '');}//retorna um numero randomico no intervalo definido por begin e endfunction getRandom (begin, end) {	return Math.round (begin + Math.random()*(end-begin));}//verifica se CPF é válidofunction validaCPF(strCPF) {    var Soma;    var Resto;    Soma = 0;           if (strCPF == "00000000000")	return false;    for (i=1; i<=9; i++)	Soma = Soma + parseInt(strCPF.substring(i-1, i)) * (11 - i);     Resto = (Soma * 10) % 11;    if ((Resto == 10) || (Resto == 11)) 	Resto = 0;    if (Resto != parseInt(strCPF.substring(9, 10)) )	return false;	Soma = 0;    for (i = 1; i <= 10; i++)       Soma = Soma + parseInt(strCPF.substring(i-1, i)) * (12 - i);    Resto = (Soma * 10) % 11;    if ((Resto == 10) || (Resto == 11)) 	Resto = 0;    if (Resto != parseInt(strCPF.substring(10, 11) ) )        return false;    return true;}//move um elemento para uma posicao x,yfunction moveTo(elem,x, y) {  elem.style.left = x+"px";   elem.style.top = y+"px";  }//retorna a posição x do elementofunction getX(elem) {  return elem.offsetLeft;  }//retorna a posição y do elementofunction getY(elem) {    return elem.offsetTop;  }